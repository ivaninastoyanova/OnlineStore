@model CreatorDetailsViewModel
@using OnlineStore.Core.Models.Creator;

@{
    ViewBag.Title = "Creator Details";
}

<h2 class="text-center">@ViewBag.Title</h2>
<hr />

<div class="container" style="display:inline">
    <div class="row">
        <div class="col-4">
            <img class="card-img-top" style="width: 20rem;"
                 src="@Model.PhotoUrl" alt="Creator Image">
        </div>
        <div class="card col-8 border-0">
            <p style="font-size:25px;"><u>@Model.FullName</u></p>
            <p>@Model.Biography</p>
            <div class="form-inline">
                @if (this.User.Identity?.IsAuthenticated ?? false)
                {
                    <a class="btn btn-warning" asp-controller="Creator" asp-action="Edit"
                       asp-route-id="@Model.Id">Edit</a>
                    <a class="ml-2 btn btn-danger" asp-controller="Creator" asp-action="Delete"
                       asp-route-id="@Model.Id" confirm="Are your sure?">Delete</a>
                    <a class="ml-2 btn btn-success" asp-controller="Creator" asp-action="Favourite"
                       asp-route-id="@Model.Id">Add to favourite</a>
                }
            </div>
        </div>
    </div>
</div>