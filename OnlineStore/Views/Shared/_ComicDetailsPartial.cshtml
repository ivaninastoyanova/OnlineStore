@model ComicAllViewModel
@using OnlineStore.Core.Models.Comic;


<div class="col-md-2">
    <div class="card mb-3">
        <img class="img-thumbnail card-img-top" src="@Model.PhotoUrl" alt="Comic Image">
        <div class="card-body text-center">
            <h4>@Model.Title</h4>
            <h6>Creator: <b>@Model.CreatorName</b></h6>
            <h6>
                Category: <b>@Model.CategoryName</b>
            </h6>

            <h6>
                Price:
                <b>@($"{Model.Price:f2}") BGN</b>
            </h6>
            <br />
            
            <a asp-controller="Comic" asp-action="Details" asp-route-id="@Model.Id"
               class="btn btn-warning">Details</a>


            @if (this.User.IsInRole("Admin"))
            {
                <p></p>
                <a asp-controller="Comic" asp-action="Edit" asp-route-id="@Model.Id"
                   class="btn btn-warning">Edit</a>
                <a asp-controller="Comic" asp-action="Delete" asp-route-id="@Model.Id"
                   class="btn btn-danger" confirm="Are your sure?">Delete</a>
                <p></p>
            }
            <div class="form-inline">
                <a asp-controller="Cart" asp-action="Purchase" asp-route-id="@Model.Id"
                   class="btn btn-success">Purchase</a>
            </div>
        </div>
    </div>
</div>